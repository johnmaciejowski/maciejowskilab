.DS_Store
content/
*-draft-*.md

# Page copy sources live in the working folder and must never be published
# (markdown -> HTML by hand). publications.json IS committed: it is the single
# source for the publications build (tools/build_publications.py writes static
# HTML into publications.html and the Home block).
/*.md
!/README.md
